'use client'

import { createTheme } from '@mui/material'

export const theme = createTheme({
  cssVariables: true,
  typography: {
    allVariants: {
      fontFamily: 'Inter, sans-serif',
    },
  },
})
